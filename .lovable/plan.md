Replace course posters with newly uploaded images.

1. Replace `poster-a1-fizic.webp` with the A1 fizic image the user just uploaded.
2. Wait for the A2 fizic upload, then replace `poster-a2-fizic.webp` with it.
3. Do NOT run a build or publish after replacements — the user explicitly requested no build at this stage.

Scope: image asset replacement only. No code changes, no route changes, no build.